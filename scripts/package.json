{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "master-access": "tsx ./src/apply-master-access.ts",
    "test": "tsx --test src/*.test.ts",
    "check-links": "tsx ./src/check-consultant-links.ts",
    "check-login": "tsx ./src/check-login.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:"
  }
}
